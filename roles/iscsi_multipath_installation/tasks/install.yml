---
- name: Install required packages
  yum:
    name:
      - iscsi-initiator-utils
      - device-mapper-multipath
    state: present

- name: Enable and start iSCSI initiator service
  systemd:
    name: iscsid
    enabled: yes
    state: started

- name: Enable and start multipathd service
  systemd:
    name: multipathd
    enabled: yes
    state: started
